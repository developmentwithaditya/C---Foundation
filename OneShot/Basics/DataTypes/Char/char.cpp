#include <iostream>
using namespace std;

int main()
{
    // Char Datatype = Is a type of container that store a sigle digits of Alphabets 'a', 'b', 'C', 'D' and Special characters '!', '@', '&' so on.. Char is occupied 1 bytes of memory.

    char alphabet_A = 'A';
    char astrisk = '*';
    cout << "This is a Alphabet: " << alphabet_A << endl;
    cout << "This is Special Character: " << astrisk << endl;
    return 0;
}